{
  "name": "qemu-lpc4088-controller",
  "version": "1.0.3",
  "description": "",
  "main": "dist/index.js",
  "scripts": {
    "qemu_test": "ts-node --files src/qemu_integrity_tests/index.ts",
    "qemurun": "ts-node --files src/apprunner/index.ts",
    "start": "ts-node --files src/index.ts",
    "build": "tsc",
    "postinstall": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Mahmut Karaca",
  "license": "MIT",
  "peerDependencies": {
    "typescript": "^3.9.7"
  },
  "dependencies": {
    "crc-32": "^1.2.0",
    "ts-node": "^9.1.1"
  },
  "devDependencies": {
    "@types/node": "^14.10.2"
  }
}
